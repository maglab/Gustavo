library(biomaRt)
library(GenomicRanges)
library(stringr)
out_dir <- "D:/Respaldo_PHD/Nature/Data/Retrieved/Genes_and_diseases/Ranges"
if (!dir.exists(out_dir)) dir.create(out_dir, recursive = TRUE)
# -------------------------------------------------------------------------
# Connection to Ensembl (GRCh38 / hg38)
# Nota: por defecto Ensembl apunta a GRCh38. Si quieres reproducibilidad estricta,
# puedes fijar una versión de Ensembl con useEnsembl(..., version = XX).
mart <- useEnsembl(
biomart = "genes",
dataset = "hsapiens_gene_ensembl",
version = 115
)
# 1) actualiza Bioconductor (si tu R lo permite)
BiocManager::install(version = "3.18")
# quitar dbplyr actual
remove.packages("dbplyr")
# instalar una versión estable compatible
install.packages(
"https://cran.r-project.org/src/contrib/Archive/dbplyr/dbplyr_2.3.4.tar.gz",
repos = NULL,
type = "source"
)
